import '@testing-library/jest-dom';
import { render, fireEvent, screen } from '@testing-library/react';

import RequisitionConfigurationInput from './RequisitionConfigurationInput';

test('shows a RequisitionConfigurationInput', () => {
    // render(<RequisitionConfigurationInput></RequisitionConfigurationInput>);
    // fireEvent.click(screen.getByLabelText(text));
    // fireEvent.click(screen.getByText(text));
    // expect(screen.queryByText(testMessage)).toBeNull()
    // expect(screen.getByText(testMessage)).toBeInTheDocument()
    // expect(screen.queryByText(text)).toBeVisible();
});
